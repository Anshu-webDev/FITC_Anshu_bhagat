#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

int main(void) {

	int t,n,b,zc,i;
	long int sum;
	scanf("%d",&t);
	while(__)
	{ sum __;
	  scanf("%d",&n);
	  zc=0;
	  for(__)
	  { scanf("%d",&b);
	    if(__) zc++;
	    else sum =__ ;
	  }
	  if(sum<100)
	  { printf("NO\n");
	  }
	  else
	  { if(sum____) printf("YES\n");
	    else if((___ ) __ ( __-zc)) printf("YES\n");
	    else printf("NO\n");
	  }
	}
	
}

